# Optimize engagement and reflex behavior

Evolve `choose_engagement(state, options) -> option_id`. Small sessions should
not receive unnecessary teaching. Truncation or pressure activates affordances;
lean models receive fewer. Confirmed starvation densifies, while repeated
positive outcomes may bypass teaching but never evidence or addresses. Change
only the `EVOLVE-BLOCK`.

Each option exposes `id`, `provides`, `safe`, and the five efficiency metrics.
