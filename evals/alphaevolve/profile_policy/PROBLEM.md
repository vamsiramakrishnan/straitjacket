# Select the correct digest profile

Evolve `choose_profile(state, options) -> option_id`. Exact family detection is
a hard completion gate: a cheaper misclassification is not an improvement.
Binary magic must win before text and ambiguous prose must fall back to text.
Then minimize classifier/render overhead. Change only the `EVOLVE-BLOCK`.

Each option exposes `id`, `provides`, `safe`, and the five efficiency metrics.
