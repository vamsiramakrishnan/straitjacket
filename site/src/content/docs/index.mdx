---
title: straitjacket
description: Let your coding agent read everything it needs, without paying for it on every turn or losing the one line that mattered.
template: splash
hero:
  title: straitjacket
  tagline: >-
    Your coding agent can read the whole test log, the whole build output, the
    whole repository — and still have room left to think. You stop paying for
    the same bytes every turn, and the one line that mattered is still there
    an hour later.
  actions:
    - text: Get started
      link: /straitjacket/start/getting-started/
      icon: right-arrow
      variant: primary
    - text: See how it works
      link: /straitjacket/start/how-it-works/
      icon: open-book
      variant: secondary
    - text: GitHub
      link: https://github.com/vamsiramakrishnan/straitjacket
      icon: github
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## The tax you're already paying

One `pytest -q` can put **300,000 tokens** into your agent's transcript. You
don't pay for that once — every turn afterwards re-sends it. A routine
`mcp__github__list_commits` costs about **19,800 tokens per round**, forever.

Then the window fills, compaction runs, and it silently deletes the one failing
line you needed. No warning. No trace it was ever there. You find out when the
agent confidently tells you something it can no longer check.

That is the tax: **you pay more, the agent gets dumber, and the evidence you
needed is the first thing thrown overboard.**

## What straitjacket does for you

<CardGrid>
  <Card title="Your agent stops going blind" icon="rocket">
    It can run the noisy suite, tail the long build, and sweep the big
    repository without spending its whole window on the output. The room it
    needs for reasoning is still there on turn twenty.
  </Card>
  <Card title="You stop re-paying for the same bytes" icon="seti:clock">
    A 304,113-token log costs about **210 tokens** in your transcript — and
    stays that size no matter how loud the command was. It's charged once, not
    on every round for the rest of the session.
  </Card>
  <Card title="Nothing you needed vanishes quietly" icon="approve-check">
    Every byte left out of the summary keeps an exact address. The failing
    assertion, the one anomalous line, the stack frame three screens down —
    all still retrievable long after compaction would have dropped them.
  </Card>
  <Card title="Answers you can check" icon="magnifier">
    When the agent tells you something, it can show you where it came from —
    the same address returns the same bytes tomorrow, next week, on another
    machine. No "I recall seeing", no rerunning the suite to confirm.
  </Card>
  <Card title="It works with the agent you already use" icon="puzzle">
    Antigravity, Claude Code, and Codex. One command, merged into your existing
    config, never clobbering it. You don't change how you work.
  </Card>
  <Card title="Longer sessions actually stay coherent" icon="random">
    Because the window stays small, your agent keeps its cache warm and its
    plan intact. The difference shows up as fewer turns, not just fewer tokens.
  </Card>
</CardGrid>

## Why that's worth caring about

<div style="border: 2px solid var(--sl-color-white); box-shadow: var(--sj-shadow); margin-bottom: 1rem;">
  <img className="sj-dark" src="/straitjacket/diagrams/econ.svg" width="100%" alt="Without: one pytest run puts 304,113 tokens in the transcript, re-sent on every later turn. With: a ~210-token digest plus addresses; raw bytes stay on disk and the rest of the window is free." style="display:block" />
  <img className="sj-light" src="/straitjacket/diagrams/econ-light.svg" width="100%" alt="Without: one pytest run puts 304,113 tokens in the transcript, re-sent on every later turn. With: a ~210-token digest plus addresses; raw bytes stay on disk and the rest of the window is free." style="display:block" />
</div>

Context is not just a cost line — it's the agent's working memory. A window
full of last hour's log is a window that can't hold your problem. Every token
you spend re-reading `pytest` output is a token not spent on the bug.

The gains compound in the direction you want: adopting the solution ladder
measured **−28% turns, −33% wall-clock time, −17% cost** on the same tasks.
Fewer tokens is the mechanism. Finishing sooner is the result.

Nothing here is claimed without a receipt. Every steering change ships only
after a measured A/B or head-to-head, and all of it is published in
[`evals/`](https://github.com/vamsiramakrishnan/straitjacket/tree/main/evals).
House rule: *receipts before doctrine*.

## How it compares

Everyone in this space is solving the same tax. The question is what they
charge you for it.

| Instead of… | What it costs you | What you get here |
|---|---|---|
| **Letting the window fill** | you pay for every byte, every turn, and quality falls as it fills | the log is charged once, at a fixed small size |
| **Compaction / summarization** | rewrites your history and throws evidence away permanently — you can't get it back | clearing is lossless: the evidence is secured first, then the window is freed |
| **RAG / vector memory** | probabilistic recall with no provenance — you can't tell if it's real | exact addresses: the same coordinate returns the same bytes forever |
| **Rewriting wire proxies** (Headroom) | silent evidence drops — in our head-to-head, 347,595 tokens became 68 with no trace | every elided byte stays on disk and addressed; **96.5–98.1% cache hit vs their 80.6–84.2%** |
| **Bash-hook filters** (rtk) | lossy on the paths that succeeded; no way to go back and look | budgets that spend bytes on failures and starve success noise |
| **Terse-prompting styles** (Caveman) | destroys evidence to save tokens — the quiet-needle problem | cite instead of quote, with a handle that resolves |

We benchmarked or stress-tested each of these, kept the good idea, and recorded
what each still does better than us. That last part matters and is written
down: Headroom's zero-integration generality, rtk's 15-host reach and sub-10ms
binary, and more, in
[Comparisons](/straitjacket/guides/comparisons/).

<div style="border: 2px solid var(--sl-color-white); box-shadow: var(--sj-shadow); margin: 1rem 0;">
  <img className="sj-dark" src="/straitjacket/diagrams/field-treemap.svg" width="100%" alt="A treemap of the field: Headroom, rtk, Caveman, Compaction, RAG/vectors, Ponytail, Maki and wozcode. Each tile names the tool's one good idea, its limitation, and — on an amber strip — the lossless form straitjacket adopted." style="display:block" />
  <img className="sj-light" src="/straitjacket/diagrams/field-treemap-light.svg" width="100%" alt="A treemap of the field: Headroom, rtk, Caveman, Compaction, RAG/vectors, Ponytail, Maki and wozcode. Each tile names the tool's one good idea, its limitation, and — on an amber strip — the lossless form straitjacket adopted." style="display:block" />
</div>

## Try it in three commands

```bash
pip install -e .    # from a clone (PyPI release pending); Python 3.11+
cd your-repo
ctx wrap setup      # Antigravity, Claude Code and Codex are now harnessed
```

Then run anything noisy through it — no agent needed to see the difference:

```bash
ctx run -- pytest -q
```

`ctx wrap setup` is idempotent and non-destructive: it merges into your
existing config, names every file it writes, verifies itself, and exits
non-zero if anything is wrong rather than claiming success.

## Then, how it actually works

Only once you care. The short version: raw bytes stop at the gate, land in an
immutable local store, and your transcript gets a small summary plus exact
addresses back into the original.

<div style="border: 2px solid var(--sl-color-white); box-shadow: var(--sj-shadow);">
  <img className="sj-dark" src="/straitjacket/diagrams/flow.svg" width="100%" alt="Tool output hits the birth gate; every raw byte lands in an immutable artifact store; the transcript gets a bounded, span-addressed digest; ctx get returns the exact bytes any time." style="display:block" />
  <img className="sj-light" src="/straitjacket/diagrams/flow-light.svg" width="100%" alt="Tool output hits the birth gate; every raw byte lands in an immutable artifact store; the transcript gets a bounded, span-addressed digest; ctx get returns the exact bytes any time." style="display:block" />
</div>

A token passes four points in its life, and every mechanism in the system
attaches to exactly one of them: whether output can flood **at birth**, what
crosses the wire **on entry**, what's allowed to **stay resident**, and what
the model **emits** back.

[**How it works**](/straitjacket/start/how-it-works/) walks one command through
the whole system in plain language, in about ten minutes. If you'd rather see
it applied to your situation, [Use cases](/straitjacket/guides/use-cases/) is
organised by the failure you're hitting, not by the feature that fixes it.
