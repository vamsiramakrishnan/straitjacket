---
title: straitjacket
description: Artifact-backed context containment harness for coding agents.
template: splash
hero:
  title: straitjacket
  tagline: >-
    Unbounded tool output becomes an immutable artifact plus a bounded,
    deterministic, span-addressed digest. The transcript keeps small digests
    and exact addresses instead of the raw bytes, so your context window and
    per-turn cost stay down and any omitted byte is retrievable later.
  actions:
    - text: Get started
      link: /straitjacket/start/getting-started/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/vamsiramakrishnan/straitjacket
      icon: github
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## How it works

Raw bytes stop at the gate. The transcript carries the digest and its
addresses, and any address resolves back to the exact original bytes later.

<div style="border: 2px solid var(--sl-color-white); box-shadow: var(--sj-shadow);">
  <img className="sj-dark" src="/straitjacket/diagrams/flow.svg" width="100%" alt="Tool output hits the birth gate; every raw byte lands in an immutable artifact store; the transcript gets a bounded, span-addressed digest; ctx get returns the exact bytes any time." style="display:block" />
  <img className="sj-light" src="/straitjacket/diagrams/flow-light.svg" width="100%" alt="Tool output hits the birth gate; every raw byte lands in an immutable artifact store; the transcript gets a bounded, span-addressed digest; ctx get returns the exact bytes any time." style="display:block" />
</div>

## The four gates

A token passes four points in its life. One artifact store handles all four as
gates, and every shipped mechanism attaches to exactly one of them.

<CardGrid>
  <Card title="1 · Birth" icon="rocket">
    Can this output flood at the source? `ctx run`/`seq`/`eval` capture,
    supervised backgrounding, head/tail evidence windows, deterministic
    digest profiles, failure-asymmetric budgets.
  </Card>
  <Card title="2 · Entry" icon="magnifier">
    What actually crosses the wire? Tier-0 byte-exact observer proxy,
    shape-dispatched PostToolUse gate for every faucet, scorecards.
  </Card>
  <Card title="3 · Residence" icon="seti:clock">
    What may stay, and for how long? Session read ledger, window-pressure
    loop, priced steering, epoch-latched lossless rescue, checkpoints.
  </Card>
  <Card title="4 · Emission" icon="pencil">
    What does the model put back? Emission governor tiers, cite-don't-quote,
    solution ladder + backward planning, deliverable metrics.
  </Card>
</CardGrid>

## The measured results

<div style="border: 2px solid var(--sl-color-white); box-shadow: var(--sj-shadow); margin-bottom: 1rem;">
  <img className="sj-dark" src="/straitjacket/diagrams/econ.svg" width="100%" alt="Without: one pytest run puts 304,113 tokens in the transcript, re-sent on every later turn. With: a ~210-token digest plus addresses; raw bytes stay on disk and the rest of the window is free." style="display:block" />
  <img className="sj-light" src="/straitjacket/diagrams/econ-light.svg" width="100%" alt="Without: one pytest run puts 304,113 tokens in the transcript, re-sent on every later turn. With: a ~210-token digest plus addresses; raw bytes stay on disk and the rest of the window is free." style="display:block" />
</div>

Every steering change ships only after a measured A/B or head-to-head, all
recorded in
[`evals/`](https://github.com/vamsiramakrishnan/straitjacket/tree/main/evals).
A 304,113-token log becomes ~210 model-visible tokens, and the one anomalous
line survives verbatim with an exact retrieval coordinate. Every branch a
mechanism takes records what it did, and those records compile into the next
epoch's steering policy.
